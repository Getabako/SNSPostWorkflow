{
  "name": "sns-workflow",
  "version": "1.0.0",
  "description": "Instagram自動投稿ワークフロー - カレンダーから画像生成、Publer CSV作成まで",
  "main": "index.js",
  "type": "module",
  "scripts": {
    "analyze-homepage": "node src/analyze-homepage.js",
    "generate-calendar": "node src/generate-calendar.js",
    "generate-images": "node src/generate-images.js",
    "workflow": "npm run analyze-homepage && npm run generate-calendar",
    "workflow-with-images": "npm run analyze-homepage && npm run generate-calendar && npm run generate-images"
  },
  "keywords": [
    "instagram",
    "automation",
    "social-media",
    "github-actions"
  ],
  "author": "",
  "license": "MIT",
  "dependencies": {
    "@google/generative-ai": "^0.24.1",
    "dotenv": "^16.3.1",
    "jsdom": "^23.0.0"
  }
}
